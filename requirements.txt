# Web framework
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0

# Database
sqlmodel>=0.0.21
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.29.0
alembic>=1.13.0

# Settings & validation
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# Auth
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# Encryption (for storing API keys in DB)
cryptography>=43.0.0

# Redis & Celery (background jobs, call registry, rate limiting)
redis[asyncio]>=5.0.0
celery>=5.4.0

# HTTP
httpx>=0.27.0

# Voice / AI providers — let pipecat-ai resolve its own transitive deps
pipecat-ai[silero]==0.0.61
google-genai>=1.14.0
groq>=0.12.0
openai>=1.55.0
sarvamai>=0.1.0

# LangGraph (state machine agent)
langgraph>=0.2.55
langchain-core>=0.3.21

# Telephony
twilio>=9.3.0

# Utilities
python-multipart>=0.0.12
tenacity>=9.0.0
certifi>=2024.0.0
